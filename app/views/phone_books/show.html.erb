<p id="notice"><%= notice %></p>

<h1><%= @phone_book.title %></h1>
<p>URL: <code><%= phone_book_url(@phone_book, format: :xml) %></code></p>

<%= render partial: 'phone_book_entries/table', locals: { entries: @entries, show_phone_book: false } %>

<%= link_to t('new_title', model: PhoneBookEntry.model_name.human), new_phone_book_entry_path, class: 'btn btn-primary' %>
<%= link_to t('edit'), edit_phone_book_path(@phone_book), class: 'btn btn-secondary' %>
<%= link_to t('back'), phone_books_path, class: 'btn btn-secondary' %>
